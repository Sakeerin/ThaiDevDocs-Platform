# name: Content Check

# on:
#   pull_request:
#     paths:
#       - 'content/**'
#       - 'scripts/**'
#       - '.github/workflows/content-check.yml'
#   push:
#     branches:
#       - main
#     paths:
#       - 'content/**'
#       - 'scripts/**'

# jobs:
#   validate:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: 20
#           cache: npm

#       - name: Install dependencies
#         run: npm ci

#       - name: Validate MDX frontmatter
#         run: npm run content:validate

#       - name: Check stale articles
#         run: npm run content:stale

#       - name: Content QA
#         run: npm run content:qa

#       - name: Type check
#         run: npm run types:check

#   links:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Link Checker
#         uses: lycheeverse/lychee-action@v2
#         with:
#           args: --verbose --no-progress './content/**/*.mdx' './README.md' './.github/CONTRIBUTING.md'
#           fail: true
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#   preview-note:
#     runs-on: ubuntu-latest
#     if: github.event_name == 'pull_request'
#     steps:
#       - name: Vercel preview reminder
#         run: |
#           echo "Vercel preview deploys automatically when the repo is connected to Vercel."
#           echo "Check the PR checks for the preview URL before merging."
